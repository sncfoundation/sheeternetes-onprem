# Copy to .skctl.env and source-load automatically by skctl / kubelet.sh.
# On-prem: point at the LOCAL apiserver instead of an Apps Script /exec URL.
WEBAPP_URL=http://localhost:8787
TOKEN=CHANGE_ME_super_secret
